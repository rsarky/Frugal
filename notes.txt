Create these tables.

create table expense ( user_id,timestamp,amount,mode,comments)
begin
create table expense( user_id integer(11), date_time  timestamp, category set('Food','Travel','Utilities','Laundry','Others') , amount integer(7) , mode set('cash','ewallet','card'), comments varchar(200),primary key(user_id,date_time),foreign key(user_id) references users(id))
create table income( user_id integer(11), date_time timestamp, source set('Salary','Parents','Others'), amount integer(7), mode set('cash','ewallet','bank'),comments varchar(200),primary key(user_id,timestamp))
/


TODO
> income
> Alter table expense to include transaction id